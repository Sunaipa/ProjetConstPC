{% extends "base-layout.html.twig" %}

{% block content %}
    
    {# Affichage de block de configue PAR configue #}

    <div class="row">
        {% for item in items %}
            <div class="p-3 col-3">
            </div>
        {% endfor %}
    </div>

{% endblock %}